<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wonder Forum</title>
  <link rel="stylesheet" href="/style.css">

<h1><%= categoryName.charAt(0).toUpperCase() + categoryName.slice(1) %> Posts</h1>

<% if (user) { %>
  <p>Welcome, <%= user.username %> | <a href="/logout">Logout</a></p>
  <a href="/create-post?category=<%= categoryName %>">Create Post</a>
<% } else { %>
  <p><a href="/login">Login</a> to create a post</p>
<% } %>

<hr>

<% if (posts.length === 0) { %>
  <p>No posts yet in this category.</p>
<% } else { %>
  <% posts.forEach(post => { %>
    <div style="border:1px solid #ccc; padding:10px; margin:10px 0;">
      <h3><%= post.title %></h3>
      <p><%= post.content %></p>
      <small>Posted by: <%= post.UserId %></small>
    </div>
  <% }) %>
<% } %>
